define(['core', 'io'], function (core, io) {

	// var hub = (function (core, io) {

	// })(core, io || window.io);

	// // var MessageHub = function () {
	// // 	this.io = io || window.io;
	// // };

	// // _.extend(MessageHub.prototype, {
		
	// // 	init: function () {
	// // 		var socket = this.io.connect();

	// // 		socket.on('connect', function () {
	// // 			alert('connected!');
	// // 		});
	// // 	}

	// // });

	// // return MessageHub;

	// return function () {
		
	// };

});